public class Subtraction implements GetUserValue {

    @Override
    public double calculateDouble(double operand1, double operand2) {

        double subtraction = operand1 - operand2;
        return subtraction;
    }
}
